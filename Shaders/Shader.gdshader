shader_type canvas_item;

//uniform sampler2D screen_texture : hint_screen_texture, repeat_disable, filter_nearest;
//
//void fragment() {
	//vec4 color = texture(screen_texture, SCREEN_UV);
	//
	////if (color.rgb == vec3(1, 1, 1))
		////COLOR = vec4(1, 0, 0, 1);
	//COLOR = color * vec4(1.5, 0, 0, 1);
//}
